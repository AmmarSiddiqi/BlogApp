import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#fff",
    },
  },
  background: {
    default: "#e6d8d8",
  },
});

export default theme;
